switch(expression) {
	case condition1:
		 . . . // code block gets executed if case condition1 is true
		 break; // program continues after the switch statement
	case condition2:
		 . . . // code block gets executed if case condition2 is true
	default:
		. . . // if both given conditions are false.
}
